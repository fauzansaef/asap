#MYSQL DB
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:127.0.0.1}:3306/db_asap?useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username=user_asap
spring.datasource.password=P@55w0rd!@#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.use-new-id-generator-mappings=false

#Hikari connection pool
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.connectionTimeout=300000

#hide schema in swagger-ui
springdoc.swagger-ui.defaultModelsExpandDepth=-1

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = none
spring.jpa.show-sql=false
springdoc.api-docs.path=/api-docs

logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

relawan.app.jwtSecret= 4SaP@ppS3cr3tK3y
relawan.app.jwtExpirationMs= 86400000
server.port=8888

file.directory = ./files/asap/
#file.directory = D:/PROJECT/Luar/files/asap/

#MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=20MB
# Max Request Size
spring.servlet.multipart.max-request-size=20MB